<%= @page = :discovery %>

<h2>Showing lessons</h2>
<% if @lessons.present? %>
  <ul data-role="listview" data-theme="g">
    <% @lessons.each do |lesson| %>
      <li>
        <a href="<%= lesson_path(lesson) %>">
          <%= lesson.title %>
          <br/><%= lesson.start %> <--> <%= lesson.end %> @ <%= lesson.time %>
        </a>
      </li>
    <% end %>
  </ul>
<% else %>
  No lessons yet.
<% end %>